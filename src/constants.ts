/** Color for points that are part of at least one bonus combo. */
export const COMBO_POINT_COLOR = "#a855f7";

/** LocalStorage key for "How to use" dismissal. */
export const HOW_TO_DISMISSED_KEY = "matt-planner-hide-howto";

/** Default map centre when there are no points. */
export const DEFAULT_CENTER: [number, number] = [48.219, 20.405];
